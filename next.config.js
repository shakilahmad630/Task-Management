/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Task-Management',
  images: {
    unoptimized: true,
  },
  reactStrictMode: true,
}

module.exports = nextConfig
